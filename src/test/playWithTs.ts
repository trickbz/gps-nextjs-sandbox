// Use this file to play with Type Script, e.g. resolving interview tasks
// You can use 'npx tsx <path_to_file>' command to run this file.
// IMPORTANT: Pay attention. this file doesn't have import or export,
// so it is not treated by TS as a module. If you define here any variable,
// and the variable with the same name is defined in the similar file (without
// import or export), then will be in the same scope for TS (but they not will
// be added to the window or global objects).
console.log('play with TS here');
